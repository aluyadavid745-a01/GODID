services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: godid
      POSTGRES_USER: godid
      POSTGRES_PASSWORD: change-me
    ports:
      - "5432:5432"
    volumes:
      - godid-postgres:/var/lib/postgresql/data

volumes:
  godid-postgres:
